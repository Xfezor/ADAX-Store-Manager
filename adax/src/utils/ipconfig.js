// En este archivo se define la IP y el puerto del servidor de la API
export const ip = "localhost"; 
export const port = "80";
export const protocol = "${protocol}s"; // Cambia a "${protocol}s" si es necesario